// Cash flow ratio
let cash = 1000, currentLiabilities = 500;
let cashFlowRatio = cash / currentLiabilities;

// Net income
let revenues = 1000, expenses = 500;
let netIncome = revenues - expenses;

// Total assets
let liabilities = 1000, equity = 500;
let totalAssets = liabilities + equity;

// Net income (using profit margin and sales)
let profit = 1000, sales = 500;
let netIncomeProfit = profit * sales;

// Average
let num1 = 7, num2 = 9, num3 = 2;
let average = (num1 + num2 + num3) / 3;

// Discount
let price = 150, discountPercent = 30;
let discountedPrice = price - (price * (discountPercent / 100));

// Age limit
let age = 20;
let isEligible = age > 18 && age < 30;

// Exponential
let base = 2, exponent = 3;
let powerResult = Math.pow(base, exponent);

// Remainder
let dividend = 10, divisor = 4;
let remainder = dividend % divisor;